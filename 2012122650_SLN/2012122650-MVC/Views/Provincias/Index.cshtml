@model IEnumerable<_2012122650_ENT.Entities.Provincia>

@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>
    <p>
        @Html.ActionLink("Create New", "Create")
    </p>
    <table class="table">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Departamento.departamento)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.nomProvincia)
            </th>
            <th></th>
        </tr>
    
    @foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Departamento.departamento)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.nomProvincia)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id=item.ProvinciaId }) |
                @Html.ActionLink("Details", "Details", new { id=item.ProvinciaId }) |
                @Html.ActionLink("Delete", "Delete", new { id=item.ProvinciaId })
            </td>
        </tr>
    }
    
    </table>
</body>
</html>
