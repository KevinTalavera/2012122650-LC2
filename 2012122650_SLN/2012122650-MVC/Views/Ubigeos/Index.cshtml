@model IEnumerable<_2012122650_ENT.Entities.Ubigeo>

@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>
    <p>
        @Html.ActionLink("Create New", "Create")
    </p>
    <table class="table">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Distrito.nomDistrito)
            </th>
            <th></th>
        </tr>
    
    @foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Distrito.nomDistrito)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id=item.UbigeoId }) |
                @Html.ActionLink("Details", "Details", new { id=item.UbigeoId }) |
                @Html.ActionLink("Delete", "Delete", new { id=item.UbigeoId })
            </td>
        </tr>
    }
    
    </table>
</body>
</html>
